/*
 * @name "Lists"
 */

/*
 * @identifier names
 * @type list
 */
var ["Ryan", "John", "Jane"]

for name in names {
    println(name)
}

for i, _ in names {
    println(i)
}

for i, name in names {
    println(i + ". " + name)
}