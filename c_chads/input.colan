domath := proc(x : int; y : int) {
    i := 2 * y;
    z := 12 * i;
    print("Math works!"; ((x - (y >= 5)*8) - z) );
};

dounary := proc(x : int) {
    print(!-(x * x + 12));
};

main := proc () {
    domath(12; 2);
    dounary(12);
};
