domath := proc(x : int; y : int) int {
    i := 2 * y;
    z := 12 * i;
    print("Math works!"; ((x - (y >= 5)*8) - z) );
};

dounary := proc(x : int) int {
    print(!-(x * x + 12));
};

main := proc () int {
    domath(12; .2);
    dounary(032);
};
