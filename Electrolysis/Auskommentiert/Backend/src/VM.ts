import * as AST from './grammar';
import {CommentProvider} from "./CommentProvider";

export class VM {
    constructor(astProvider : CommentProvider) {
        
    }
    public async run() {

    }
    private traverse(this: VM, ast : AST.Comment) {

    }
}