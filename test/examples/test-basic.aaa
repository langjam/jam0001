// a number
let x = 4;
// another number
let y = 5;
// yet another number
let stuff = add(<number>);

print(<number>);
print(<arithmetic>(<number>));

let fn = { |x y|: z
    // arg0
    x;
    // arg1
    y;
    let z = add(<arg>);
};

print(fn(1 2));
