// dbg(AST.name);
let x = 5;

// AST.initializer = fmt("{} + 7", AST.initializer.code);
// AST.name = "z";
// dbg(AST.code);
let y = 5;
print z;


// fn rename(name) {
//     AST.name = name;
// }


// please rename this to "func".
fn test(a, b, c) {
    return a + b + c;
}

print func(1, 2, 3);


